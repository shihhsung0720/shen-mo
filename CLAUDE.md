# 森SHEN 內容系統 — Claude 工作守則

> 這份是**通用規則**、給任何接手的人 / AI 都適用。
> 森SHEN 專屬設定（品牌、團隊、私密 key 名稱）放在 `CLAUDE.local.md`。

---

## 第一層：紅線（違反就停下、不要動手）

1. **保養品法規紅線**
   絕對不可寫「療效」「治療」「醫美等級」「無副作用」「根治」「修復受損」
   等宣稱、不可暗示醫療效果。任何文案產出後、先過 `01-brand/voice.md`
   裡的「禁用詞清單」自查、再交給人看。

2. **不在資料裡的事實不要編**
   成分、認證（ECOCERT、SGS）、檢驗結果、創辦故事、原料來源、價格、
   數據、若 `01-brand/` 或 `02-inventory/` 裡沒寫、要問人、不能腦補。

3. **改之前先講方案**
   下列動作強制先列計畫等使用者點頭：
   - 刪檔、改 CSV 結構、覆蓋整個 `02-inventory/`
   - 一次改超過 3 個檔
   - 改 `CLAUDE.md` / `CLAUDE.local.md` 本身
   - 任何 `git push --force` / `git reset --hard` 類不可逆操作

4. **只改要求的部分**
   不要順手改其他貼文記錄、不要重排目錄、不要把 markdown 改成自己
   喜歡的格式、不要修「看起來怪但其實是刻意」的中文用法。

5. **修問題前先想規則 / 設定 / 資料**
   題材重複 → 先看 `03-axis-map/` 是否該補一條軸、不要直接寫新 prompt
   蓋過去。文案常踩雷 → 先補 `voice.md` 禁用詞、不要每次手動改。
   寫新程式是最後手段。

---

## 第二層：怎麼找東西（資料地圖）

| 想知道 | 看哪裡 | 誰說了算 |
|---|---|---|
| 我們是誰、品牌怎樣 | `01-brand/positioning.md` | 這份 |
| 目標客群是誰 | `01-brand/audience.md` | 這份 |
| 文案語氣 / 禁用詞 | `01-brand/voice.md` | 這份 |
| 過去拍過什麼 | `02-inventory/content-log.csv` | CSV |
| 主題分布統計 | `02-inventory/theme-stats.md` | **衍生、不要手改** |
| 現在做哪幾條軸 | `03-axis-map/current-axes.md` | 這份 |
| 可延伸哪些軸 | `03-axis-map/extended-axes.md` | 這份 |
| 不碰的題材 | `03-axis-map/taboo.md` | 這份 |
| 下一支拍什麼 | `04-topics/backlog.md` | 這份 |
| 已拍過的記錄 | `04-topics/shot.md` | 這份 |
| Claude 的工作範本 | `06-prompts/` | 這份 |

**衍生檔不要手改**：
- `02-inventory/theme-stats.md` 是從 CSV 跑出來的、改 CSV 就好。
- 任何標「衍生」「auto-generated」的檔、都從來源檔重跑、不要直接編輯。

---

## 第三層：日常習慣

- **切換任務開新對話**
  「盤點過去內容」「發想新題材」「寫腳本」是三件不同的事、
  不要在同一個對話裡混、context 會打架、Claude 會抓錯重點。

- **覺得方向歪了用 rewind**
  發現 Claude 抓錯品牌調性、寫出販賣焦慮的句子、
  別在錯的方向上繼續疊、回到上一輪重來、會更省時間。

- **警告要走完四階段**
  發現重複題材 / 違規詞、不能只印一行警告：
  1. **警告**：說明哪裡有問題
  2. **自動建議替代**：給出可改寫方案
  3. **通知小編**：標記在輸出檔頂部、不能埋進中段
  4. **擋住新任務**：未處理前不能繼續發想下一批

  只印警告不修、3 個月後就變噪音、大家會學會忽略。

- **新犯的錯先記軟規則**
  寫進 `CLAUDE.local.md` 的「踩過的坑」、反覆出現再升級到本檔的紅線、
  不要一次寫死、避免規則臃腫又沒人讀。

- **產出前一定先讀品牌設定**
  每次發想題材 / 寫腳本、第一步必須讀完 `01-brand/` 三份檔、
  不要靠對話前文記憶、記憶會漂、檔案才是真相。
